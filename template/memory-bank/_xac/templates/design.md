---
title: <Design: page or flow name>
kind: design
tier: semantic
pinned: false
expires_at:
entities: []
authority: canonical
evidence: []
ticket:
current:
---

# <Design: page or flow name>

<!-- ticket: the tickets/ page this design serves. Optional — design can exist without one.
     current: the approved mock version, e.g. design/checkout/v2/.

     The mock is a spec, not source. Developers reimplement it in the project's stack and
     existing components. Tokens and interaction semantics transfer; mock markup does not. -->

## Direction

<!-- The register in three adjectives, who it serves, references adopted and what from each. -->

## Tokens

<!-- Font families, weights, and where to install them from. Palette. Spacing scale.
     Must match the CSS custom properties at the top of the mock. -->

## Pages and states covered

<!-- Each page, and which states it shows: empty, loading, error, disabled, validation,
     hover/focus, mobile. -->

## Interactions

<!-- What responds to what: navigation, transitions, keyboard behaviour, form feedback. -->

## Iteration history

<!-- - v1 -> v2: what changed and why. Versions are never overwritten. -->

## Open questions
